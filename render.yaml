# render.yaml (crear en raíz del repo)
services:
  - type: web
    name: pyquant-webhooks
    env: python
    buildCommand: "pip install flask"
    startCommand: "python utils/webhook_receiver.py"
    envVars:
      - key: WEBHOOK_SECRET
        sync: false