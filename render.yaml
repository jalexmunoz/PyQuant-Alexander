services:
  - type: web
    name: pyquant-webhooks
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn -w 2 -b 0.0.0.0:$PORT utils.webhook_receiver:app"
    envVars:
      - key: WEBHOOK_SECRET
        value: pyquant_shadow_2025_xyz123